diff --git a/lib/client.js b/lib/client.js
--- a/lib/client.js
+++ b/lib/client.js
@@ -9,6 +9,15 @@ window.__ModuleLoader__.load({
 		let react_jsx_runtime = require("react/jsx-runtime");
 		let react = require("react");
 		let _deepseek_ai_dsh_client_ui_primitives = require("@deepseek-ai/dsh-client-ui-primitives");
+		/** Candidate icons the desktop adds beyond the upstream ReferenceIcon kinds. */
+		function renderDesktopCandidateIcon(icon) {
+			const Icon = icon === "skill" ? _deepseek_ai_dsh_client_ui_primitives.IconSkillOutline16 : icon === "goal" ? _deepseek_ai_dsh_client_ui_primitives.IconGoalOutline16 : icon === "command" ? _deepseek_ai_dsh_client_ui_primitives.IconCodeOutline16 : void 0;
+			if (Icon === void 0) return (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.ReferenceIcon, {
+				kind: icon,
+				size: 16
+			});
+			return (0, react_jsx_runtime.jsx)(Icon, { size: 16 });
+		}
 		//#region ../../context/file-reference/src/grammar.ts
 		/**
 		* Extract an `@path` or `@"path with spaces` token at the cursor. An `@`
@@ -177,16 +186,17 @@ window.__ModuleLoader__.load({
 			}
 			return out;
 		}
-		/** True when every group is ready with zero items (the auto-close condition). */
+		/** True when every group is ready with zero items (the desktop empty-hint condition). */
 		const allReadyEmpty = (groups) => groups.every((g) => g.status === "ready" && g.items.length === 0);
 		/**
 		* Pure menu reducer. `hit` opens a new generation over the seeded roster
 		* (null hit closes); `source-settled` outside the current generation, the
-		* open menu, or the roster is dropped; a settlement or failure leaving every
-		* group ready-and-empty (or no groups) auto-closes; `source-failed` silently
-		* removes the group (the shell logs); `move` cycles the highlight across
-		* ready items; `hover` parks it on one ready item (pointer and keyboard
-		* share the single highlight — last input wins).
+		* open menu, or the roster is dropped; a source failure that empties the
+		* roster (every source failed) auto-closes, while an all-empty
+		* settlement stays open so the render layer can show the localized empty
+		* hint; `source-failed` silently removes the group (the shell logs); `move`
+		* cycles the highlight across ready items; `hover` parks it on one ready
+		* item (pointer and keyboard share the single highlight — last input wins).
 		*
 		* @param state - Current menu state.
 		* @param ev - Menu event.
@@ -216,7 +226,6 @@ window.__ModuleLoader__.load({
 						status: "ready",
 						items
 					} : g);
-					if (allReadyEmpty(groups)) return closed(state);
 					const highlight = validHighlight(state.highlight, groups) ?? firstHighlight(groups);
 					return {
 						...state,
@@ -228,7 +237,7 @@ window.__ModuleLoader__.load({
 					if (!state.open || ev.generation !== state.generation) return state;
 					if (!state.groups.some((g) => g.source === ev.source)) return state;
 					const groups = state.groups.filter((g) => g.source !== ev.source);
-					if (groups.length === 0 || allReadyEmpty(groups)) return closed(state);
+					if (groups.length === 0) return closed(state);
 					const highlight = validHighlight(state.highlight, groups) ?? firstHighlight(groups);
 					return {
 						...state,
@@ -377,21 +386,23 @@ window.__ModuleLoader__.load({
 					this.dismiss();
 					return;
 				}
-				const match = this.deps.roster.sources(hit.trigger).find((item) => item.name === source);
+				const roster = this.deps.roster.sources(hit.trigger);
+				const match = roster.find((item) => item.name === source);
 				if (match === void 0) {
 					this.dismiss();
 					return;
 				}
+				const launched = roster.filter((item) => item === match || item.launchers?.includes(source));
 				this.stopFetch();
 				this.hit = hit;
 				this.launcher.set(source);
-				this.menu.set(seedGroups(this.menu.getSnapshot(), [match]));
+				this.menu.set(seedGroups(this.menu.getSnapshot(), launched));
 				this.reduce({
 					type: "hit",
 					hit
 				});
-				this.refreshHeaders(hit, [match]);
-				this.fetchCandidates(hit, [match]);
+				this.refreshHeaders(hit, launched);
+				this.fetchCandidates(hit, launched);
 			}
 			/**
 			* Pointer pick from MenuView: route the clicked candidate through onPick
@@ -846,7 +857,7 @@ window.__ModuleLoader__.load({
 		}
 		//#endregion
 		//#region \0dsh-css:/Users/xipian/workspace/wbs/deepseek-harness-desktop/deepseek-harness/packages/client/ui-input-trigger/src/client/MenuView.module.css.mjs
-		const css = ".iRJKyq_menu{z-index:100;--dsh-scrollbar-thumb:var(--dsw-alias-scrollbar-bg-l2);--dsh-scrollbar-thumb-hover:var(--dsw-alias-scrollbar-hover-l2);background:var(--dsw-specific-menu);--dsw-elevation-stroke-color:var(--dsw-alias-border-l1);max-height:320px;box-shadow:var(--dsw-elevation-prominent);border:0;border-radius:20px;flex-direction:column;padding:4px;display:flex;position:absolute;bottom:calc(100% + 4px);left:0;right:0;overflow:hidden}.iRJKyq_viewport{flex-direction:column;min-height:0;display:flex;overflow-y:auto}.iRJKyq_item{cursor:pointer;width:100%;min-height:40px;color:var(--dsw-alias-label-primary);text-align:left;background:0 0;border:none;border-radius:10px;align-items:center;gap:8px;padding:8px 10px;font-size:14px;line-height:22px;display:flex}.iRJKyq_item.iRJKyq_active{background:var(--dsw-alias-interactive-bg-hover)}.iRJKyq_sectionTitle{min-height:26px;color:var(--dsw-alias-label-tertiary);flex:none;padding:6px 10px 2px;font-size:12px;font-weight:500;line-height:18px}.iRJKyq_sectionTitle:not(:first-child){margin-top:4px}.iRJKyq_itemIcon{width:16px;height:16px;color:var(--dsw-alias-label-tertiary);flex:none;justify-content:center;align-items:center;display:inline-flex}.iRJKyq_itemName{text-overflow:ellipsis;white-space:nowrap;flex:none;max-width:40%;overflow:hidden}.iRJKyq_itemDescription{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsw-alias-label-tertiary);flex:1;overflow:hidden}.iRJKyq_trailing{flex:none;align-items:center;gap:4px;margin-left:auto;display:inline-flex}.iRJKyq_drillHintText{color:var(--dsw-alias-label-caption);white-space:nowrap;font-size:11px;line-height:18px;display:none}.iRJKyq_drillHint{background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-caption);border-radius:4px;padding:0 5px;font-family:inherit;font-size:11px;line-height:18px;display:none}.iRJKyq_item.iRJKyq_active .iRJKyq_drillHintText,.iRJKyq_item.iRJKyq_active .iRJKyq_drillHint{display:inline-flex}.iRJKyq_drill{width:20px;height:20px;color:var(--dsw-alias-label-caption);border-radius:4px;flex:none;place-items:center;display:inline-grid}.iRJKyq_drill:hover{background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-primary)}.iRJKyq_groupTitle{color:var(--dsw-alias-label-tertiary);padding:8px 10px;font-size:12px;line-height:16px}.iRJKyq_skeletonRow{box-sizing:border-box;align-items:center;min-height:40px;padding:8px 10px;display:flex}.iRJKyq_skeletonBar{background:var(--dsw-alias-bg-skeleton);border-radius:4px;height:20px;animation:2s cubic-bezier(.36,0,.64,1) infinite iRJKyq_dsh-menu-skeleton}@keyframes iRJKyq_dsh-menu-skeleton{0%{opacity:1}40%{opacity:.6}80%,to{opacity:1}}.iRJKyq_crumbs{border-bottom:.5px solid var(--dsw-alias-border-l1);flex-wrap:wrap;flex:none;align-items:center;gap:2px;margin-bottom:2px;padding:4px 4px 6px;display:flex}.iRJKyq_crumb{max-width:40%;color:var(--dsw-alias-label-tertiary);cursor:pointer;text-overflow:ellipsis;white-space:nowrap;background:0 0;border:none;border-radius:6px;flex:0 auto;padding:2px 6px;font-family:inherit;font-size:12px;line-height:18px;overflow:hidden}.iRJKyq_crumb:hover{background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-primary)}.iRJKyq_crumbCurrent,.iRJKyq_crumbCurrent:hover{color:var(--dsw-alias-label-primary);cursor:default;background:0 0}.iRJKyq_crumbSeparator{color:var(--dsw-alias-label-caption);flex:none;display:inline-flex}";
+		const css = ".iRJKyq_menu{z-index:100;--dsh-scrollbar-thumb:var(--dsw-alias-scrollbar-bg-l2);--dsh-scrollbar-thumb-hover:var(--dsw-alias-scrollbar-hover-l2);background:var(--dsw-specific-menu);--dsw-elevation-stroke-color:var(--dsw-alias-border-l1);max-height:320px;box-shadow:var(--dsw-elevation-prominent);border:0;border-radius:20px;flex-direction:column;padding:4px;display:flex;position:absolute;bottom:calc(100% + 4px);left:0;right:0;overflow:hidden}.iRJKyq_viewport{flex-direction:column;min-height:0;display:flex;overflow-y:auto}.iRJKyq_item{cursor:pointer;width:100%;min-height:40px;color:var(--dsw-alias-label-primary);text-align:left;background:0 0;border:none;border-radius:10px;align-items:center;gap:8px;padding:8px 10px;font-size:14px;line-height:22px;display:flex}.iRJKyq_item.iRJKyq_active{background:var(--dsw-alias-interactive-bg-hover)}.iRJKyq_sectionTitle{min-height:26px;color:var(--dsw-alias-label-tertiary);flex:none;padding:6px 10px 2px;font-size:12px;font-weight:500;line-height:18px}.iRJKyq_sectionTitle:not(:first-child){margin-top:4px}.iRJKyq_itemIcon{width:16px;height:16px;color:var(--dsw-alias-label-tertiary);flex:none;justify-content:center;align-items:center;display:inline-flex}.iRJKyq_itemName{text-overflow:ellipsis;white-space:nowrap;flex:none;max-width:40%;overflow:hidden}.iRJKyq_itemDescription{text-overflow:ellipsis;white-space:nowrap;min-width:0;color:var(--dsw-alias-label-tertiary);flex:1;overflow:hidden}.iRJKyq_trailing{flex:none;align-items:center;gap:4px;margin-left:auto;display:inline-flex}.iRJKyq_drillHintText{color:var(--dsw-alias-label-caption);white-space:nowrap;font-size:11px;line-height:18px;display:none}.iRJKyq_drillHint{background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-caption);border-radius:4px;padding:0 5px;font-family:inherit;font-size:11px;line-height:18px;display:none}.iRJKyq_item.iRJKyq_active .iRJKyq_drillHintText,.iRJKyq_item.iRJKyq_active .iRJKyq_drillHint{display:inline-flex}.iRJKyq_drill{width:20px;height:20px;color:var(--dsw-alias-label-caption);border-radius:4px;flex:none;place-items:center;display:inline-grid}.iRJKyq_drill:hover{background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-primary)}.iRJKyq_groupTitle{color:var(--dsw-alias-label-tertiary);padding:8px 10px;font-size:12px;line-height:16px}.iRJKyq_emptyState{color:var(--dsw-alias-label-tertiary);padding:8px 10px;font-size:13px;line-height:20px}.iRJKyq_skeletonRow{box-sizing:border-box;align-items:center;min-height:40px;padding:8px 10px;display:flex}.iRJKyq_skeletonBar{background:var(--dsw-alias-bg-skeleton);border-radius:4px;height:20px;animation:2s cubic-bezier(.36,0,.64,1) infinite iRJKyq_dsh-menu-skeleton}@keyframes iRJKyq_dsh-menu-skeleton{0%{opacity:1}40%{opacity:.6}80%,to{opacity:1}}.iRJKyq_crumbs{border-bottom:.5px solid var(--dsw-alias-border-l1);flex-wrap:wrap;flex:none;align-items:center;gap:2px;margin-bottom:2px;padding:4px 4px 6px;display:flex}.iRJKyq_crumb{max-width:40%;color:var(--dsw-alias-label-tertiary);cursor:pointer;text-overflow:ellipsis;white-space:nowrap;background:0 0;border:none;border-radius:6px;flex:0 auto;padding:2px 6px;font-family:inherit;font-size:12px;line-height:18px;overflow:hidden}.iRJKyq_crumb:hover{background:var(--dsw-alias-interactive-bg-hover);color:var(--dsw-alias-label-primary)}.iRJKyq_crumbCurrent,.iRJKyq_crumbCurrent:hover{color:var(--dsw-alias-label-primary);cursor:default;background:0 0}.iRJKyq_crumbSeparator{color:var(--dsw-alias-label-caption);flex:none;display:inline-flex}";
 		const tagId = "@deepseek-ai/dsh-client-ui-input-trigger/MenuView.module.css";
 		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId) + "]") === null) {
 			const tag = document.createElement("style");
@@ -865,6 +876,7 @@ window.__ModuleLoader__.load({
 			"drillHint": "iRJKyq_drillHint",
 			"drillHintText": "iRJKyq_drillHintText",
 			"dsh-menu-skeleton": "iRJKyq_dsh-menu-skeleton",
+			"emptyState": "iRJKyq_emptyState",
 			"groupTitle": "iRJKyq_groupTitle",
 			"item": "iRJKyq_item",
 			"itemDescription": "iRJKyq_itemDescription",
@@ -957,7 +969,7 @@ window.__ModuleLoader__.load({
 					role: "listbox",
 					"aria-label": t("suggestions.aria"),
 					"aria-activedescendant": highlight !== null ? optionId(highlight.source, highlight.index) : void 0,
-					children: state.groups.map((group) => group.status === "ready" && group.items.length === 0 ? null : (0, react_jsx_runtime.jsxs)(react.Fragment, { children: [group.showGroupTitle === false || group.items.some((item) => item.section !== void 0) ? null : (0, react_jsx_runtime.jsx)("div", {
+					children: [state.groups.map((group) => group.status === "ready" && group.items.length === 0 ? null : (0, react_jsx_runtime.jsxs)(react.Fragment, { children: [group.showGroupTitle === false || group.items.some((item) => item.section !== void 0) ? null : (0, react_jsx_runtime.jsx)("div", {
 						className: MenuView_module_css_default.groupTitle,
 						role: "presentation",
 						"data-source": group.source,
@@ -1002,10 +1014,7 @@ window.__ModuleLoader__.load({
 								item.icon !== void 0 && (0, react_jsx_runtime.jsx)("span", {
 									className: MenuView_module_css_default.itemIcon,
 									"aria-hidden": true,
-									children: (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.ReferenceIcon, {
-										kind: item.icon,
-										size: 16
-									})
+									children: renderDesktopCandidateIcon(item.icon)
 								}),
 								(0, react_jsx_runtime.jsx)("span", {
 									className: MenuView_module_css_default.itemName,
@@ -1043,7 +1052,11 @@ window.__ModuleLoader__.load({
 								})
 							]
 						})] }, optionId(group.source, index));
-					})] }, group.source))
+					})] }, group.source)), state.groups.length > 0 && allReadyEmpty(state.groups) ? (0, react_jsx_runtime.jsx)("div", {
+						className: MenuView_module_css_default.emptyState,
+						role: "presentation",
+						children: t(state.hit !== null && state.hit.trigger === "@" ? state.hit.query === "" ? "empty.at" : "empty.at.query" : "empty.slash")
+					}) : null]
 				})]
 			});
 		}
@@ -1052,7 +1065,7 @@ window.__ModuleLoader__.load({
 		/**
 		* `slash.menu` namespace dictionaries: group titles keyed by source name
 		* (the lookup chain returns the key itself, so an unknown source shows its
-		* raw name), the pending row, and the listbox and header aria labels.
+		* raw name), the pending row, the all-empty hint, and the listbox and header aria labels.
 		*/
 		/** Simplified Chinese dictionary (the key-set source of truth). */
 		const zh = {
@@ -1060,6 +1073,9 @@ window.__ModuleLoader__.load({
 			"skill": "技能",
 			"subagent": "子智能体",
 			"loading": "正在加载…",
+			"empty.at": "当前工作区没有可引用的文件",
+			"empty.at.query": "没有匹配的文件或会话",
+			"empty.slash": "没有匹配的指令",
 			"drill.aria": "进入目录",
 			"drill.hint": "进入目录",
 			"drill.key": "Tab",
@@ -1072,6 +1088,9 @@ window.__ModuleLoader__.load({
 			"skill": "Skills",
 			"subagent": "Subagents",
 			"loading": "Loading…",
+			"empty.at": "No referenceable files in the current workspace",
+			"empty.at.query": "No matching files or sessions",
+			"empty.slash": "No matching commands",
 			"drill.aria": "Browse folder",
 			"drill.hint": "Browse folder",
 			"drill.key": "Tab",
diff --git a/lib/types/client/locales.d.ts b/lib/types/client/locales.d.ts
--- a/lib/types/client/locales.d.ts
+++ b/lib/types/client/locales.d.ts
@@ -1,7 +1,7 @@
 /**
  * `slash.menu` namespace dictionaries: group titles keyed by source name
  * (the lookup chain returns the key itself, so an unknown source shows its
- * raw name), the pending row, and the listbox and header aria labels.
+ * raw name), the pending row, the all-empty hint, and the listbox and header aria labels.
  */
 /** Simplified Chinese dictionary (the key-set source of truth). */
 export declare const zh: {
@@ -9,6 +9,9 @@ export declare const zh: {
     skill: string;
     subagent: string;
     loading: string;
+    'empty.at': string;
+    'empty.at.query': string;
+    'empty.slash': string;
     'drill.aria': string;
     'drill.hint': string;
     'drill.key': string;
@@ -23,6 +26,9 @@ export declare const en: {
     skill: string;
     subagent: string;
     loading: string;
+    'empty.at': string;
+    'empty.at.query': string;
+    'empty.slash': string;
     'drill.aria': string;
     'drill.hint': string;
     'drill.key': string;
diff --git a/lib/types/types.d.ts b/lib/types/types.d.ts
--- a/lib/types/types.d.ts
+++ b/lib/types/types.d.ts
@@ -28,7 +28,7 @@ export type PickVia = 'menu' | 'space' | 'enter';
 /** What a pick asks for: resolve the candidate, or drill into it in place. */
 export type PickAction = 'pick' | 'drill';
 /** Leading glyph token of one menu candidate, mapped to its SVG by the menu view. */
-export type InputTriggerCandidateIcon = 'file' | 'folder' | 'session';
+export type InputTriggerCandidateIcon = 'file' | 'folder' | 'session' | 'skill' | 'goal' | 'command';
 /** One menu candidate. Pure display data — zero behavior declaration. */
 export interface InputTriggerCandidate {
     readonly name: string;
@@ -131,6 +131,11 @@ export interface InputTriggerSource {
     readonly name: string;
     /** Menu group display order (lower = higher in the list; default 0). */
     readonly order?: number;
+    /**
+     * Programmatic source launchers this source accompanies. The source keeps
+     * its own candidates and pick behavior while sharing the launcher's menu.
+     */
+    readonly launchers?: readonly string[];
     /** Whether the menu renders the source-title row; defaults to true. */
     readonly showGroupTitle?: boolean;
     candidates(session: ClientSessionContext, req: CandidateRequest): Promise<readonly InputTriggerCandidate[]>;
